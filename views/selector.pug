extends layout

block content
  h1= title
  - var image_names=["Mastiff.jpg","beagle.jpg","bull.jpg","german.jpg","lab.jpg"]
  - for(let i=0;i<3;i++){
     - let rand=Math.floor(Math.random()*5)
     img(src=`/images/${image_names[rand]}` alt=`${rand}` height="300px" width="300px") 
  - }