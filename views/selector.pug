extends layout

block content
  h1= title
  - var image_names=["1.jpg","2.jpg","3.jpg","5.jpg","6.jpg"]
  - for(let i=0;i<3;i++){
     - let rand=Math.floor(Math.random()*5)
     img(src=`/images/${image_names[rand]}` alt=`${rand}` height="300px" width="300px") 
  - }